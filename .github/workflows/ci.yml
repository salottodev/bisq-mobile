name: BisqApps

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  # --- Ktlint Check (runs in parallel with build) ---
  ktlint:
    name: Ktlint Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Azul Zulu)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'zulu'

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Create fake local.properties
        run: |
          # Omit KEYSTORE_PATH: this job does not package a release APK, so it
          # never exercises signingConfigs.release. A dummy path used to fail
          # configuration.
          echo "KEYSTORE_PASSWORD=yourKeystorePassword" > local.properties
          echo "KEY_ALIAS=yourKeyAlias" >> local.properties
          echo "CLI_KEY_ALIAS=yourCliKeyAlias" >> local.properties
          echo "KEY_PASSWORD=yourKeyPassword" >> local.properties
          echo "CLI_KEY_PASSWORD=yourCliKeyPassword" >> local.properties

      - name: Run ktlint check
        run: ./gradlew ktlintCheck -Pkotlin.native.ignoreDisabledTargets=true

  # --- Common Build & Test (runs for all PRs, including forks) ---
  build:
    name: Build & Test (No Secrets)
    strategy:
      matrix:
        # macos-26 (Xcode 26 / iOS 26 SDK) is required for the iOS link step: Compose Multiplatform 1.11's
        # ui-uikit references iOS 26-only UIKit symbols (e.g. UIViewLayoutRegion), so linking the iOS test
        # binary on the older macos-14 image (Xcode 15/16) fails with "Undefined symbols for architecture arm64".
        os: [ubuntu-latest, macos-26]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21 (Azul Zulu)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'zulu'

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Cache Kotlin/Native (konan) (Mac only)
        if: startsWith(matrix.os, 'macos')
        uses: actions/cache@v4
        with:
          path: |
            ~/.konan
          key: ${{ runner.os }}-konan-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-konan-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Create fake local.properties
        run: |
          # Omit KEYSTORE_PATH: this job does not package a release APK, so it
          # never exercises signingConfigs.release. A dummy path used to fail
          # configuration.
          echo "KEYSTORE_PASSWORD=yourKeystorePassword" > local.properties
          echo "KEY_ALIAS=yourKeyAlias" >> local.properties
          echo "CLI_KEY_ALIAS=yourCliKeyAlias" >> local.properties
          echo "KEY_PASSWORD=yourKeyPassword" >> local.properties
          echo "CLI_KEY_PASSWORD=yourCliKeyPassword" >> local.properties

      # Stub google-services.json so the google-services plugin is applied in CI.
      # No secrets needed — placeholder IDs only. Lets us catch regressions in
      # the plugin-applied build path (resource generation, manifest merge) on
      # every PR including forks. Real Firebase config (production or staging)
      # is provisioned only in release workflows from a private secret.
      - name: Stub google-services.json for CI
        run: |
          cat > apps/clientApp/google-services.json <<'EOF'
          {
            "_comment": "STUB — generated by ci.yml. Real config is per-environment, gitignored.",
            "project_info": {
              "project_number": "000000000000",
              "project_id": "bisq-mobile-ci-stub",
              "storage_bucket": "bisq-mobile-ci-stub.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:000000000000:android:0000000000000000000000",
                  "android_client_info": { "package_name": "network.bisq.mobile.client" }
                },
                "oauth_client": [],
                "api_key": [{ "current_key": "STUB_API_KEY_NOT_FOR_PROD" }],
                "services": { "appinvite_service": { "other_platform_oauth_client": [] } }
              },
              {
                "client_info": {
                  "mobilesdk_app_id": "1:000000000000:android:1111111111111111111111",
                  "android_client_info": { "package_name": "network.bisq.mobile.client.debug" }
                },
                "oauth_client": [],
                "api_key": [{ "current_key": "STUB_API_KEY_NOT_FOR_PROD" }],
                "services": { "appinvite_service": { "other_platform_oauth_client": [] } }
              }
            ],
            "configuration_version": "1"
          }
          EOF
          echo "Wrote stub apps/clientApp/google-services.json"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          yes | sdkmanager --licenses || true
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0" "ndk;26.1.10909125"

      - name: Set up Xcode (Mac only)
        if: startsWith(matrix.os, 'macos')
        run: |
          sudo xcode-select -s /Applications/Xcode.app || true
          xcodebuild -version

      - name: Run Android Tests (Ubuntu only)
        if: startsWith(matrix.os, 'ubuntu')
        env:
          CI: true
        # --no-build-cache forces test tasks to actually execute rather than being restored as
        # build-cache hits. Kover's per-test coverage (.ic) binary is only produced when the test
        # JVM runs with the Kover agent; a cache-hit test task produces no coverage, which then
        # makes the separate koverVerify step see near-zero coverage intermittently (flaky failures).
        run: ./gradlew clean test --no-configuration-cache --no-build-cache -x :apps:nodeApp:test --info

      - name: Run Kover Verification (Ubuntu only, excluding nodeApp)
        if: startsWith(matrix.os, 'ubuntu')
        run: ./gradlew koverVerify koverXmlReport --no-configuration-cache -x :apps:nodeApp:koverVerify -x :apps:nodeApp:koverXmlReport -x :apps:nodeApp:koverGenerateArtifact --info

      - name: Upload Kover XML report (Ubuntu only)
        if: startsWith(matrix.os, 'ubuntu')
        uses: actions/upload-artifact@v4
        with:
          name: kover-report
          retention-days: 7
          path: build/reports/kover/report.xml

      - name: Upload diff coverage report (Ubuntu only)
        id: coverage_check
        if: always() && startsWith(matrix.os, 'ubuntu') && github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: diff-coverage-report-no-secrets
          retention-days: 30
          path: build/reports/diff-cover/diff-cover.html

      - name: Install CocoaPods (Mac only)
        # Replaces the previous `generateDummyFramework` step. The dummy
        # framework was enough when no module needed real pods at link time,
        # but the analytics work (#525) introduces Sentry-KMP, whose
        # cinterop bindings reference symbols from the Sentry Cocoa SDK.
        # Without a real `pod install`, the K/N linker fails the iOS test
        # binaries with `ld: framework 'Sentry' not found`. `podInstall`
        # is a superset of `generateDummyFramework`; macos-26 runners have
        # CocoaPods preinstalled, so no extra setup is required.
        if: startsWith(matrix.os, 'macos')
        run: ./gradlew :apps:clientApp:podInstall

      - name: Android compile-only (Mac only)
        if: startsWith(matrix.os, 'macos')
        run: ./gradlew :apps:clientApp:assembleDebug -x test -x lint

      - name: Run iOS Tests (Intel runner)
        if: startsWith(runner.os, 'macOS') && runner.arch == 'X64'
        run: ./gradlew shared:presentation:iosX64Test

      - name: Run iOS Tests (Apple Silicon runner)
        if: startsWith(runner.os, 'macOS') && runner.arch == 'ARM64'
        run: ./gradlew shared:presentation:iosSimulatorArm64Test

      - name: Run domain iOS Tests (Intel runner)
        if: startsWith(runner.os, 'macOS') && runner.arch == 'X64'
        env:
          CI: true
        run: |
          ./gradlew :shared:domain:iosSimulatorX64Test --info

      - name: Run domain iOS Tests (Apple Silicon runner)
        if: startsWith(runner.os, 'macOS') && runner.arch == 'ARM64'
        env:
          CI: true
        run: |
          ./gradlew :shared:domain:iosSimulatorArm64Test --info

      - name: Run NSE decryption tests (Mac only)
        if: startsWith(matrix.os, 'macos')
        run: swift support/test_nse_decryption.swift

  pr-coverage:
    name: PR Coverage
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Kover XML report
        uses: actions/download-artifact@v4
        with:
          name: kover-report
          path: build/reports/kover

      - name: Check diff coverage
        run: |
          pip install diff-cover==10.2.0

          # Use smart diff coverage analyzer
          chmod +x scripts/analyze-diff-coverage.sh
          ./scripts/analyze-diff-coverage.sh origin/main build/reports/kover/report.xml


  # --- Node app tests (needs secrets for Maven dependencies) ---
  node-maven:
    name: Node App Tests (Needs Secrets)
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository # skip only fork PRs

    env:
      CI: true
      MAVEN_URL: ${{ secrets.MAVEN_URL }}
      MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
      MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21 (Azul Zulu)
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'zulu'

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          yes | sdkmanager --licenses || true
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0" "ndk;26.1.10909125"

      - name: Create fake local.properties
        run: |
          # Omit KEYSTORE_PATH: this job does not package a release APK, so it
          # never exercises signingConfigs.release. A dummy path used to fail
          # configuration.
          echo "KEYSTORE_PASSWORD=yourKeystorePassword" > local.properties
          echo "KEY_ALIAS=yourKeyAlias" >> local.properties
          echo "CLI_KEY_ALIAS=yourCliKeyAlias" >> local.properties
          echo "KEY_PASSWORD=yourKeyPassword" >> local.properties
          echo "CLI_KEY_PASSWORD=yourCliKeyPassword" >> local.properties

      - name: Run Node App Tests
        run: ./gradlew :apps:nodeApp:testDebugUnitTest :apps:nodeApp:testReleaseUnitTest --info
